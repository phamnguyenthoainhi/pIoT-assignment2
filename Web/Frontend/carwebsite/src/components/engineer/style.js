const style = (theme) => ({
    root: {
        backgroundColor: theme.color.green.lightest,
        padding: "40px"
    },
    avatar: {
        backgroundColor: "whitesmoke",
    },
})
export default style;